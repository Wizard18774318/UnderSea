using UnityEngine;

// Unused — kept to avoid missing script errors on any existing prefabs.
public class ShrinkAfterDelay : MonoBehaviour
{
    public void Init(float delay, float shrinkDuration = 0f) { }
}

